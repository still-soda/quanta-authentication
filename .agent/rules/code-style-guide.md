---
trigger: always_on
---

1. 请遵循项目结构，将类型文件放置在 `/types` 目录下，将配置文件放置在 `/config` 目录下。同样的如果发现零散的类型定义和配置定义，请将它们移入合适的位置。
2. 文件体积不得过大，如果超过 400 行，请拆分组件或函数。
3. 不得在代码中加入冗余的注释，注释应言简意赅、精炼准确。同样的如果发现有用于注释，请进行消除。
